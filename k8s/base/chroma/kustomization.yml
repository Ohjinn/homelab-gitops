apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yml
  - pvc.yml
  - deployment.yml
  - service.yml
  - middleware.yml
  - ingress.yml
  - indexer-cronjob.yml
  # RAG API. Chroma 와 같은 네임스페이스에 둔다 — 같은 코퍼스를 쓰고 같은
  # basicAuth 문을 지나므로, 갈라 두면 자격증명이 둘로 늘어난다.
  - api-deployment.yml
  - api-service.yml
  - api-ingress.yml
